# AI analysis fixtures

Add six representative screenshots here before running `pnpm test:ai`:

- `product.png`
- `restaurant.png`
- `chat-task.png`
- `concert.png`
- `book-list.png`
- `unclear.png`

Start the app with `ZHIPU_API_KEY` configured, then run the live evaluation. The script verifies the expected primary intent, title and summary constraints, allowed actions, and the stable JSON response shape for every category.
